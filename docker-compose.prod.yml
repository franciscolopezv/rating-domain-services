version: '3.8'

# Production configuration using external platform-infrastructure network
# Infrastructure services (postgres, kafka, zookeeper) are provided by the platform
services:
  # Command Service
  command-service:
    build:
      context: .
      dockerfile: ./command-service/Dockerfile
    image: ratings/command-service:${VERSION:-latest}
    container_name: ratings-command-service
    restart: unless-stopped

    ports:
      - "${COMMAND_HTTP_PORT:-8081}:8081"
      - "${COMMAND_GRPC_PORT:-9090}:9090"
    environment:
      SPRING_PROFILES_ACTIVE: ${ENVIRONMENT:-prod}
      
      # Database configuration
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${WRITE_DB_NAME:-ratings_write}
      DB_USERNAME: ${WRITE_DB_USER:-ratings_user}
      DB_PASSWORD: ${WRITE_DB_PASSWORD}
      
      # Kafka configuration
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      KAFKA_PRODUCER_RETRIES: 3
      KAFKA_PRODUCER_BATCH_SIZE: 16384
      KAFKA_PRODUCER_LINGER_MS: 5
      
      # Security
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-3600}
      
      # Monitoring
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,metrics,prometheus
      MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: when_authorized
      
      # JVM settings
      JAVA_OPTS: "-Xms256m -Xmx512m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
    networks:
      - platform-infrastructure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 384M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Query Service
  query-service:
    build:
      context: .
      dockerfile: ./query-service/Dockerfile
    image: ratings/query-service:${VERSION:-latest}
    container_name: ratings-query-service
    restart: unless-stopped

    ports:
      - "${QUERY_HTTP_PORT:-8082}:8082"
    environment:
      SPRING_PROFILES_ACTIVE: ${ENVIRONMENT:-prod}
      
      # Database configuration
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${READ_DB_NAME:-ratings_read}
      DB_USERNAME: ${READ_DB_USER:-ratings_user}
      DB_PASSWORD: ${READ_DB_PASSWORD}
      
      # Kafka configuration
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      KAFKA_CONSUMER_GROUP_ID: ratings-projector-${ENVIRONMENT:-prod}
      KAFKA_CONSUMER_AUTO_OFFSET_RESET: earliest
      KAFKA_CONSUMER_MAX_POLL_RECORDS: 500
      KAFKA_CONSUMER_FETCH_MIN_BYTES: 1024
      
      # GraphQL Federation
      GRAPHQL_FEDERATION_ENABLED: true
      GRAPHQL_FEDERATION_SERVICE_NAME: ratings-service
      GRAPHQL_FEDERATION_SERVICE_URL: http://query-service:8082/graphql
      
      # Security
      JWT_SECRET: ${JWT_SECRET}
      
      # Monitoring
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,metrics,prometheus
      MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: when_authorized
      
      # JVM settings
      JAVA_OPTS: "-Xms256m -Xmx512m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
    networks:
      - platform-infrastructure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 384M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  platform-infrastructure:
    name: platform-infrastructure
    driver: bridge
    external: true